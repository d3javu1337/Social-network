странички:
    1. auth (чисто окно с двумя полями под логин и пароль, кнопка регистрации) ref -> auth +
    2. регистрация (Заполнение полей имя, фамилия, email, предпочитаемые тэги) ref -> registration +
    Странички с базой(меню -> кнопка на ленту, профиль и тд) :
    3. лента(ужатые посты с ссылкой на полный пост) ref -> feed +
    4. профиль пользователя по id/custom link и редактирование, подписки ref -> users/{id} ++-
        (сделать на view отображение кнопок в зависимости от состояния)
    5. пост по id и редактирование, лайки, комменты ref -> posts/{id}
    6. комменты к постам posts/{id}/comments/
